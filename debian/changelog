bpftune (0.4.79) unstable; urgency=medium

  * configurable v4 bucket prefix, live via 'bpftune --prefix4=N'.

    The 0.4.55 /16 merge is now a runtime knob: N is 1-32 bits of
    the remote IPv4 kept in the bucket key.  Default 16 preserves
    current behavior byte-for-byte.  Setting a different N does
    NOT wipe learned state: existing buckets keep their keys,
    their swap_score, their streaks, their rate_emas.  Only new
    sockets land under the new prefix.

    Mechanism: tuner_config_map grows from 1 to 3 slots (slot 0
    keeps exp_pct; slot 1 is prefix4; slot 2 reserved).  Same
    pinned path, so the CLI sees both slots via the same fd.
    BPF helper bucket_prefix4() reads slot 1 (clamped to 1-32)
    and bucket_key_apply_prefix() re-masks the v4-mapped key
    after the switch at both ESTABLISHED sites.

 -- cddeppe <cddeppe@users.noreply.github.com>  Sat, 26 Sep 2026 10:13:49 +0200

bpftune (0.4.78.2) unstable; urgency=medium

  * reader and collector accept v6:XXXXXXXX as a bucket key.

    _truth_write in the collector dropped any dest not shaped
    like dotted-quad, so every IPv6 swap was silently skipped
    before it reached swapscore_truth.jsonl.  The reanchor
    reader then rejected v6:... too via inet_pton(AF_INET).
    On a v6-heavy host that meant the map's swap_score and
    streak counters stalled.

    No schema change; STATE_VERSION unchanged.

 -- cddeppe <cddeppe@users.noreply.github.com>  Fri, 25 Sep 2026 18:00:00 +0000

bpftune (0.4.78.1) unstable; urgency=medium

  * tcp_conn_tuner: log IPv6 destination as dest6=
  * dashboard: IPv6 destinations in dest/read_map

 -- cddeppe <cddeppe@users.noreply.github.com>  Fri, 25 Sep 2026 16:55:12 +0200

bpftune (0.4.78) UNRELEASED; urgency=medium

  * freeze chooses target by max observed rate, not by composite
    metric min.

    best_seen_alg was "the algorithm active on the vote that returned
    the lowest composite metric."  Composite = rtt + rate + loss, so
    the choice was contaminated by low-RTT votes on algorithms the
    socket didn't actually perform well on.  Traced socket cookie
    3729: metric picked reno (metric=58321 on a low-rtt vote); cubic
    showed metric=44011 later and delivered 274 KB/s -- higher than
    any reno vote on that socket.  The freeze chose wrong.

    Across all 65 freeze events in the logs, 46 have enough pre-
    freeze samples on both candidates to compare.  20 matched
    (metric best == rate best).  26 mismatched.  Median rate
    difference when they disagreed: 1.38 MB/s; p90: 9.86 MB/s;
    max: 36 MB/s left on the table.

    Fix: track best_seen_srate and best_seen_srate_alg in
    conn_state, updated on every vote where rate_delivered exceeds
    the current max.  At freeze time, use the rate alg when it's
    populated; fall back to best_seen_alg otherwise.  Same burst
    signal the leaderboard and proof counters already use -- no
    change to rate_ema, no change to the metric, no change to the
    triggers.

    BPF-only.  conn_state is per-socket (sk_storage), not persisted.
    No STATE_VERSION bump, no dashboard or collector change.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Fri, 25 Sep 2026 12:00:00 +0000

bpftune (0.4.77) UNRELEASED; urgency=medium

  * streak counters now driven by the sustained truth file.

    0.4.76 moved swap_score to the sustained ruler but left
    bad_streak / null_streak (the pass-3 penalty inputs) on the
    in-kernel single-sample classifier, which is loss-biased.

    Fix: the reanchor worker updates streaks alongside swap_score:
        win  -> clear both streaks
        loss -> bad_streak++ (capped at 255)
        null -> null_streak++ (capped at 255)
    In-kernel streak writes disabled.  Penalty formula unchanged.

    No schema change.  No STATE_VERSION bump.  No map wipe.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Fri, 25 Sep 2026 00:30:00 +0000

bpftune (0.4.76) UNRELEASED; urgency=medium

  * swap_score reconciliation from sustained truth.

    In-kernel single-sample score write disabled.  Reanchor worker
    reads swapscore_truth.jsonl and nudges swap_score 1/8 toward
    600 per win, 100 per loss.  Streaks unchanged.  No wipe.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Thu, 24 Sep 2026 22:30:00 +0000

bpftune (0.4.75) UNRELEASED; urgency=medium

  * swap_score: sample outcome at 180s instead of 60s.

    A swap resets cwnd; slow-start takes 30-90s to climb back on
    this workload's RTTs.  The 60s sample landed mid-recovery and
    classified the dip as loss or null even when the socket settled
    higher.  Measured on 353 historical swaps with the srate series
    available:

      point sample @60s:          147 win / 149 null /  57 loss
      median srate [60,180]:      166 win / 133 null /  54 loss
      median srate [60,300]:      169 win / 132 null /  52 loss

    Moving the sample point to 180s captures most of that
    reclassification without needing the userspace median.  The
    attribution logic from 0.4.65 (cur_alg != tgt -> loss-class on
    rejection) is unchanged; only the clock changed.

    Follow-up (separate release): the collector's outcome column in
    swaps.csv still classifies on the composite metric rather than
    srate.  That column feeds the dashboard's aggregate counts; the
    sustained median is computed separately by the renderer.  Fixing
    the collector is its own change and needs testing before deploy.

    No schema change.  No STATE_VERSION bump.  Map preserved.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Thu, 24 Sep 2026 19:00:00 +0000

bpftune (0.4.74) UNRELEASED; urgency=medium

  * swap_score: balanced weights; nulls are a no-op.

    Current constants: win /16, loss /2, null /4.  Losses move the
    score 8x harder than wins.  Under this workload's mix (32% win,
    45% loss; median win ratio 495, median loss ratio 143) a
    population-average target settles at S~188 -- so from any start
    every score sank, and the map showed nearly every algorithm
    below neutral 256.

    The bad_streak / null_streak multiplier in pass 3 already
    performs the fast recency demotion (16/(16 + bad*4 + null*2));
    a target in a losing streak is downweighted within two events
    and clears the moment it wins.  Having swap_score duplicate that
    punishment, with a much heavier loss weight, was double-counting.

    New: win /8, loss /8 (same magnitude both ways), null branch
    deleted -- a null is "no evidence," not "erase."  Streak
    mechanics and pass 3 unchanged.  score_pending_rejected keeps
    using SWAP_SCORE_LOSS_DIV, so rejections drop the score by ~30
    per event instead of ~128.  No schema change.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Thu, 24 Sep 2026 18:00:00 +0000

bpftune (0.4.73) UNRELEASED; urgency=medium

  * fix 0.4.72's epoch migration.  The epoch-0 -> epoch-1 branch
    zeroed rate_ema for every algorithm on load, which emptied the
    leaderboard for ~1h.  That is the exact failure the layout/epoch
    split was introduced to prevent.  Migration is now a no-op for
    this transition: values from any prior file are trusted, and
    normal vote decay replaces them if their source had changed.

    0.4.72 shipped to all four hosts.  Its migration ran on each
    restart, and the on-disk map was overwritten on fini.  The
    rate_ema values that existed before 0.4.72 are gone; the
    leaderboard is rebuilding from votes cast on 0.4.72 and will
    not be restored by installing 0.4.73.  0.4.73 prevents the
    same wipe from happening again at the next semantics change.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Thu, 24 Sep 2026 15:30:00 +0000

bpftune (0.4.72) UNRELEASED; urgency=medium

  * cgroup attach retry.  On a fast restart the previous instance's
    cgroup binding may not have released; bpf_prog_attach returns
    EBUSY / EEXIST and the daemon comes up active but unattached
    (the "cgroup count 0" state hit repeatedly on 2026-09-24).
    Three attempts, 250ms apart, ~750ms worst case.  Non-EBUSY /
    non-EEXIST errors fail immediately as before.

  * restore rate_ok.  Removed in 0.4.71 on the theory that pass 3's
    weighted score makes the raw-rate check redundant.  Data
    collected 2026-09-24 on the heavy host:

      target rate_ema < source:  n=39   win 18%  loss 15%
      target rate_ema >= source: n=228  win 25%  loss  8%

    The blocked class has roughly double the loss rate of the rest,
    so the guard is filtering real failures.  Back in place: refuse
    a swap when the target's raw capability is below the source's,
    regardless of the target's historical score.

  * fix 0.4.70's second-best rate target.  Pass 3 computed rate_2i
    / rate_2v but the result block only wrote new_rbi / new_rbv;
    rate_second_* stayed 0 in the map and the vote-path fallback
    check (rate_second_v != 0) was permanently false.  The 0.4.70
    fallback has not fired once.  Now persists correctly.

    No STATE_VERSION change for any of the three: all fields exist,
    no schema change, map survives restart.

  * split STATE_VERSION into STATE_LAYOUT + STATE_EPOCH.
    Semantics-only releases used to wipe the map: 0.4.68, 0.4.69,
    and 0.4.71 all bumped the single version without any struct
    change, and the map spent ~2h rebuilding from zero every time.
    From now on:
      - layout compatibility is enforced by the key_size /
        value_size fields already in the header; a struct change
        still cannot load
      - semantics changes bump STATE_EPOCH, and the file loads
        with migrate_remote_host() run per entry to fix up only
        the affected fields -- swap_score, metric_value,
        metric_count, best_i/best_v, and instances all survive
    No cost this release: on-disk epoch is 0 (was 'reserved'),
    so 0.4.72 migrates once and rebuilds rate_ema over ~16 votes.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Thu, 24 Sep 2026 14:30:00 +0000
bpftune (0.4.71) UNRELEASED; urgency=medium

  * split rate_delivered into capability and health; remove rate_ok.

    Pre-0.4.68 rate_ema was sourced from the kernel burst estimate
    (tp->rate_delivered * mss / rate_interval_us).  That was the
    capability signal the leaderboard needs -- how fast an algorithm
    goes when pushed -- but it was also the swap TRIGGER's input,
    where a stale burst value made slow sockets read as fast and
    stopped swaps from firing.

    0.4.68/0.4.69 replaced everything with byte-over-wall-clock.  That
    fixed the trigger but collapsed the leaderboard: on this workload
    the app (YouTube) is the source, so every algorithm's rate_ema
    landed in the same 12-30 band regardless of what the algorithm
    could deliver.

    Fix: two locals, two jobs.
      sustained_bps  = bytes_acked+bytes_received / elapsed  (health)
      rate_delivered = burst estimate                         (capability)

    Health feeds statep->last_rate_bps (util gate, swap trigger),
    score_pending_swap (outcome attribution), and tcp_metric_calc.
    Capability feeds rate_ema (pass-3 target pick) and the proof
    counters.

    Also remove rate_ok.  Pass 3 already enforces MIN_LEADER_TRUST,
    bad_streak/null_streak penalties, and the score term.  rate_ok
    compared raw rate_ema, a different basis than pass 3's weighted
    score, so when the score ranked a lower-raw-rate algorithm as
    leader (veno, on this bucket) the gate vetoed the picker and
    sockets on the higher-rate algorithm could not be rescued.

    STATE_VERSION 18 -> 19 (rate_ema semantics changed).


 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Thu, 24 Sep 2026 10:00:00 +0000

bpftune (0.4.70) UNRELEASED; urgency=medium

  * restore the second-best fallback on the rate target.

    0.4.45 changed the swap target source from the composite metric
    (which had a second-best fallback predating the fork) to rate_ema
    (no fallback).  When a socket is already on the rate leader and
    that leader is failing for the socket, the chain reaches
    swap_tgt == s and does nothing.  Cookie 68810 (0.4.69): socket
    on vegas at 1.35 Mbps while vegas rate_ema was 81 (8.1 MB/s);
    no swap could fire on a socket 60x below its own algorithm's EMA.

    Fix: pass 3 tracks a second-best rate target by the same weighted
    score; the vote path falls back to it when the socket is already
    on the leader.  Mirrors the metric-side second_i fallback that
    has always existed.  All subsequent gates (bad_checkpoints,
    util_ok, rate_ok, settle, freeze, explore protection) still apply.

    STATE_VERSION 17 -> 18; state file auto-rejected on mismatch.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 19:00:00 +0000

bpftune (0.4.69) UNRELEASED; urgency=medium

  * rate: byte-accurate.  0.4.68 computed sustained rate from
    (segs_out + segs_in) * mss_cache / elapsed.  tcp_sock.segs_out
    counts a GSO super-segment as one segment, so the byte total was
    undercounted by the GSO factor.  Measured on heavy 2026-09-23
    against the raw kernel estimate on the same socket at the same
    instant: 64 Mbps from the byte-based computation vs 354 Mbps
    from tp->rate_delivered, a ~5.5x undercount.

    bpf_tcp_sock exposes bytes_acked and bytes_received -- real byte
    counters, retransmit-excluded.  Switch the rolling window to
    bytes_acked + bytes_received.  GSO-agnostic, TLS-agnostic, and
    measures goodput rather than sent-rate.

    conn_state.rate_win_segs is renamed to rate_win_bytes; no
    STATE_VERSION change (sk_storage is ephemeral and the value is
    only read on the initial window seed where 0 is the sentinel).

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 18:30:00 +0000

bpftune (0.4.68) UNRELEASED; urgency=medium

  * sustained-rate correction.  rate_delivered is now computed from
    actual segment movement over a >=500ms wall-clock window, not
    from tp->rate_delivered / tp->rate_interval_us.

    tp->rate_delivered is a burst estimate that updates on ACK
    arrival and can hold a value from minutes ago.  Measured on
    heavy 2026-09-23, comparing the reported rate to actual segment
    movement over the same interval:

      cookie 67545  actual  168 Kbps   reported  9.6-42.8 Mbps   (57-250x high)
      cookie 67483  actual  1.8 Mbps   reported  38 Mbps         (21x high)
      cookie 67463  actual  2.9 KB/s   reported  7.2 Mbps        (2500x high)
      cookie 67437  actual  200 Kbps   reported  7.3 Mbps        (37x high)

    Every downstream consumer reads this value: last_rate_bps, the
    d=1 and d=3 thresholds, rate_ema, swap_score's ratio, and the
    metric's rate term.  The tuner has been acting on numbers
    decoupled from actual throughput -- including on the sockets
    that most need rescuing.

    New: conn_state gains rate_win_ts_ns / rate_win_segs.  The vote
    path seeds the window on the first call, updates every >=500ms.
    Below 500ms it carries last_rate_bps unchanged.  First vote
    reports 0 so no tier acts on a one-shot reading.

    tp->rate_delivered remains in the srate printk for cross-check
    against the log until the new value has been validated.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 17:00:00 +0000

bpftune (0.4.67) UNRELEASED; urgency=medium

  * revert the 0.4.64 port-based origin gate.

    Observed: swap rate dropped from ~8.5/h (pre-0.4.64) to ~3.5/h
    (0.4.66), a 60% regression correlated with this deploy.
    Reverts the port test, restoring the 0.4.42 data_segs
    direction heuristic that was in place when the rate was correct.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 16:30:00 +0000

bpftune (0.4.67) UNRELEASED; urgency=medium

  * revert the 0.4.64 port-based origin gate.

    0.4.64 filtered remote_port==443 || 80 to skip origin-facing
    sockets.  That assumed the VPS dials a CDN on 443.  On this
    topology the client-facing stream is VPS->home, and the port
    test was dropping the sockets the tuner is meant to tune.
    Symptom: swap rate flat at ~6/h across 5%->100% explore.

    Reverts to the 0.4.42 data_segs direction heuristic.  That leaked
    ~9% on the 0.4.63 window; the leak is real but small, and prior
    to 0.4.64 the tuner worked at this rate.  Any future direction
    test must be validated against this topology: remote_port is not
    a reliable discriminator when the VPS is the server on 443.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 16:30:00 +0000

bpftune (0.4.66) UNRELEASED; urgency=medium

  * explore protection scales with the explore rate.

    EXPLORE_PROTECT_VOTES=3 protects a fresh socket from being
    swapped for its first three votes so a rare exploration pick can
    prove itself before the swap engine second-guesses it.  That
    assumption inverts when exploration is most of the traffic: at
    --exp=100 every socket is an exploration pick, so the fixed
    3-vote window protects the entire population and the swap engine
    never fires on the traffic it is meant to serve.

    Measured on the heavy host, 2026-09-23 window: 181 home-bucket
    votes, 5 swaps.  75% of votes are cookie-index 0 or 1, i.e.
    inside the protection window; only 2 of 133 structurally
    eligible votes converted.

    Fix: explore_protect_votes() reads tuner_config_map[0] and
    returns 3 at pct<=5 (original behaviour) ramping linearly to 0
    at pct>=100.  The swap-trigger site reads the helper instead of
    the constant.  No struct, map, or STATE_VERSION change.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 15:00:00 +0000

bpftune (0.4.65) UNRELEASED; urgency=medium

  * swap_score attribution fixes.  Two bugs in the way the score
    learned from swap outcomes:

    1. score_pending_swap fires at the first vote >= T+60 and reads
       the current rate.  If the socket had already rejected the swap
       and moved to another algorithm, that rate belonged to the
       replacement, not the target.  The target was scored on another
       algorithm's performance.
    2. When a new swap replaced an earlier pending one, the earlier
       target's outcome was silently dropped.  That is exactly the
       "socket rejected the target" case, which is the strongest
       negative signal a swap can produce.

    Fix: verify current alg == pending target before applying the
    rate update (otherwise loss-class), and score a pending target as
    a loss the moment a new swap replaces it.  A new helper
    score_pending_rejected() runs from all seven swap sites before
    the state is overwritten.  No struct, map, or STATE_VERSION
    change.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 14:00:00 +0000

bpftune (0.4.64) UNRELEASED; urgency=medium

  * origin gate: replace the data_segs_out * 4 < data_segs_in heuristic
    with a port test (remote_port == 443 || 80 -> skip).  The heuristic
    was supposed to catch origin-facing sockets whose rate is set by
    the CDN, not us, but it leaked ~9% of swaps (93 of 1014 on the
    0.4.63 window, heavy host).  Measured same window, direction
    classified from the met rport:
      client-facing  n=482  win 26%  null 70%  loss  5%
      origin-facing  n= 93  win 21%  null 53%  loss 26%
    Origin swaps don't just fail to help; they lose 5x as often.
    A cwnd reset perturbs the ACK pacing the CDN is already adapting
    to.  A port test does not leak.  Heuristic kept below the port
    test as a backstop for origins on non-standard ports.

  * exploration: runtime-tunable percentage for fresh sockets.  New
    BPF_MAP_DEF tuner_config_map (ARRAY, 1 entry, __u32), read at
    ESTABLISHED; the 0.4.22 tight-bucket boost is preserved (at
    pct=100 the boost is a no-op by construction).  The map is
    pinned by the daemon at /sys/fs/bpf/bpftune/tcp_conn/explore.
      bpftune --exp        prints the current value
      bpftune --exp=N      sets it (N = 0..100), live
    --exp=0 holds the picker at the leader (deterministic);
    --exp=100 explores every fresh socket.  No STATE_VERSION bump
    (the explore value is not part of tcp_conn_tuner.state).  The
    CLI records the value to /var/lib/bpftune/explore_pct; the
    daemon restores it on restart.

  * Verifier: ESTABLISHED 45346 insns / 3205 states; vote 19302 /
    1426.  The growth is the map lookup + modulo in the ESTABLISHED
    selection point.  Well under the 1M limit, but the ESTABLISHED
    path is the one the 0.4.40 lesson flagged as verifier-fragile.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Wed, 23 Sep 2026 12:00:00 +0000

bpftune (0.4.63) UNRELEASED; urgency=medium

  * reanchor: initialize stale swap_score=0 entries that have votes.
    0.4.62 set the score in set_cong(), but an algorithm already in the
    state file with score=0 (pre-0.4.62) only inits when set_cong runs
    for it -- on a bucket past coverage that means the next
    epsilon-greedy draw, not the next reanchor cycle.  The reanchor now
    does the same init during its walk so it clears within 30s.
    - userspace-only; BPF program unchanged
    - STATE_VERSION unchanged at 17

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Tue, 22 Sep 2026 23:00:00 +0000

bpftune (0.4.62) UNRELEASED; urgency=medium

  * swap_score: initialize to SWAP_SCORE_NEUTRAL (256) the first time an
    algorithm runs on a bucket.  Previously an untested algorithm held
    swap_score == 0 in the map and pass-3 substituted 256 at decision
    time.  That substitution worked for the picker but not for the state
    file or any other reader -- the dashboard showed 0, which reads as
    "tested and bottomed out".  Both meanings are now distinct in the
    map: 256 = untested-or-neutral, 0 = decayed to zero (unreachable in
    practice for the fast-decay loss branch).
    No STATE_VERSION bump.  Existing state loads unchanged; existing
    zero-score algorithms get initialized on their next socket.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Tue, 22 Sep 2026 22:30:00 +0000

bpftune (0.4.61) UNRELEASED; urgency=medium

  * swap target: replace hard exclusions with a soft penalty.  A target
    with bad_streak=2 or null_streak=3 is no longer removed from the
    candidate pool; it is down-weighted by 16/(16 + bad*4 + null*2).
    bad=2 -> 0.67x, null=3 -> 0.73x, both -> 0.53x.  A winning swap or a
    rising rate_ema still clears the streaks, and the target can climb
    back automatically.  No state change; STATE_VERSION unchanged at 17.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Tue, 22 Sep 2026 21:30:00 +0000

bpftune (0.4.60) UNRELEASED; urgency=medium

  * swap_score: asymmetric update.  A win moves the score by 1/16
    (unchanged).  A null pulls it toward neutral 256 by 1/4.  A loss
    pulls it toward the observed ratio by 1/2.  Purpose: a target with
    high historical score can no longer coast through consecutive
    nulls.  Simulated descent from a score of 340: today's rule needs
    ~20 nulls to reach 285; the new rule needs 6-8.
    - Two new constants only; struct and STATE_VERSION unchanged.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Tue, 22 Sep 2026 20:00:00 +0000

bpftune (0.4.59) UNRELEASED; urgency=medium

  * null_streak: three consecutive null swaps to an algorithm exclude it
    from the target pool.  A null is neither a loss nor a win, but it
    still costs a cwnd reset and proves nothing.  Split from bad_streak
    so loss and null can be tuned independently.

    - tcp_conn_metric: null_streak (u8), replaces the _pad0 slot
      (struct size unchanged)
    - score_pending_swap: ratio >= 1.1 clears both streaks; <= 0.9
      increments bad_streak; between, increments null_streak
    - rate_ema rising on a normal vote clears both
    - pass-3 excludes on bad_streak >= 2 or null_streak >= 3
    - STATE_VERSION 16 -> 17

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Mon, 21 Sep 2026 21:00:00 +0000

bpftune (0.4.58) UNRELEASED; urgency=medium

  * swap trust: two consecutive failed swaps to an algorithm remove it
    from the target pool.  Rising rate_ema (evidence conditions changed
    for it) or a good swap clears the streak.
    - tcp_conn_metric gains bad_streak (u8)
    - score_pending_swap increments on ratio<=0.9, clears otherwise
    - vote path clears when rate_ema rises on a vote
    - pass-3 skips any alg with bad_streak >= SWAP_BAD_STREAK_TRUST
    Observed on 2026-09-21 evening: the score-weighted pick locked
    onto bic (swap_score 414) while westwood at rate_ema 245 was
    never tested.  Without a fail-streak, a lucky first winner
    monopolizes the target slot.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Mon, 21 Sep 2026 19:30:00 +0000

bpftune (0.4.57) UNRELEASED; urgency=medium

  * Skip swaps to the socket's own algorithm.  The tier chain picked
    swap_tgt = best_alt_i unconditionally; if the socket was already
    on the metric leader and no rate leader existed, swap_tgt == s.
    set_cong() then reset cwnd for a no-op algorithm change and
    incremented swap_count toward freeze.
    Observed: cookie 45881 swapped cubic->cubic three times (d3) and
    then froze on cubic.  One-line guard inserted before the tier
    chain; freeze already guarded tgt8 != s.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Mon, 21 Sep 2026 17:30:00 +0000

bpftune (0.4.56) UNRELEASED; urgency=medium

  * FIX 0.4.55: /16 mask was applied to the ESTABLISHED key switch only,
    not the vote-path switch.  Sockets created under the merged key
    (82.43.0.0) voted under the full-IP key (CLIENT-IP); every IPv4
    vote was dropped since 0.4.55 deployed.
  * IPv4 /16 and IPv6 /32 applied to both key construction sites.
  * d2 threshold uses a 16 Mbps reference floor, so low-rate mobile
    buckets (ref ~4 Mbps) don't set a bar nothing trips.
  * swap_score feedback: score_pending_swap in BPF records post/pre
    ratio >=60s after each swap; userspace pass-3 picks the rate target
    by rate_ema * swap_score so a repeatedly-failing algorithm loses
    the race automatically.  Selection weighted in userspace because
    the 16-way loop in the vote path blows the 1M insn verifier limit.
  * STATE_VERSION 16.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Mon, 21 Sep 2026 16:00:00 +0000

bpftune (0.4.55) UNRELEASED; urgency=medium

  * IPv4 /16 bucket merge.  The tuner keyed on full remote IP; Google's
    CDN edges (142.251.150.4, 142.251.157.4, ...) fragmented into ~15
    separate buckets each with too little traffic to learn.  Only 22 of
    ~150 fleet buckets ever reach 10 votes on any algorithm.
    IPv4 keys now masked to /16 so all edges of the same CDN share one
    bucket's learned state.  IPv6 unchanged (still full-address keys).
    Individual IPs still appear verbatim on swap dest= lines, so
    per-IP analysis from the log is preserved.
  * tcp_conn_metric: add swap_score (u16, unused in this release).
    Reserved for 0.4.56's swap-outcome feedback.
  * STATE_VERSION 15 -> 16.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Mon, 21 Sep 2026 11:00:00 +0000

bpftune (0.4.54) UNRELEASED; urgency=medium

  * three changes to reduce wasted swaps and fix the trigger operands.

    1. MIN_LEADER_TRUST 3 -> 10.  Only 22 of ~150 buckets on the fleet
       ever reach 10 votes on ANY algorithm.  A trusted leader from 3
       samples is noise.
    2. exploring protection.  set_cong() marks a socket exploring when
       the assigned alg is neither the metric nor the rate leader.
       Vote path counts votes on that alg; swap chain skips until
       EXPLORE_PROTECT_VOTES (3) votes are cast.  Prior behavior
       allowed swap after 1 vote, so exploration contributed one
       sample per event.
    3. trigger operands composite -> rate.  margin_met and desperate
       compare last_rate_bps against rate_best_v, not last_metric
       against best_alt.  Old composite thresholds 1.5x / 2.0x become
       rate 66% / 25% of leader.  Over a 30-swap measured sample,
       rate-based tiers scored 74% win / 4% loss while the composite
       tier scored 57% win.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sat, 19 Sep 2026 19:00:00 +0000

bpftune (0.4.53) UNRELEASED; urgency=medium

  * new srate companion printk on every vote, alongside midsamp.
    Analysis of 212 swaps on 2026-09-19: 78% had no post-swap
    midsamp and were therefore unmeasurable.  Reason: midsamp
    only fires on rung crossings (1K..1M segs).  Sockets past the
    last rung only vote via the 60s time-check, and the time-check
    path never emitted a raw delivered rate.  So long-lived
    sockets that produced a swap were untrackable.
        srate cookie=N alg=N srate=N
    fires on every vote regardless of which branch triggered it,
    carrying the same rate_delivered * mss * 1e6 / rate_interval_us
    the midsamp line uses.  Log-only; no state change.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sun, 20 Sep 2026 01:00:00 +0000

bpftune (0.4.52) UNRELEASED; urgency=medium

  * new swap tier d=3: slow-vs-leader on rate, bypassing the composite.
    Cookie 19233 (2026-09-19, illinois->yeah d=2) showed the composite
    penalizing success: rate_term improved 7.43M -> 5.08M (raw 38 Mbps
    -> 156 Mbps) while rtt_term rose 0.36 -> 2.64M from induced
    queueing, so composite val went 8.55M -> 7.72M -- worse than
    pre-swap.  The composite is not a valid outcome signal when
    throughput improvements fill the bottleneck queue.
        slow_vs_leader = (last_rate_bps * 100 <
                          rate_best_v * 100000 * RATE_TRIGGER_PCT)
    with RATE_TRIGGER_PCT = 50 (under half of the best algorithm's
    demonstrated rate).  Guards: rate_ok, util_ok, !frozen.  Sits
    between d=2 and moderate.  Depth labeled d=3.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sat, 19 Sep 2026 14:00:00 +0000

bpftune (0.4.51) UNRELEASED; urgency=medium

  * new swap tier d=2: fire on slow-vs-reference, not just slow-vs-leader.
    Observe: cookie 18631, cubic, delivered 26.9 Mbps (5% of the
    536 Mbps reference) for 5.6 minutes and never crossed a checkpoint
    capable of tripping the moderate tier (which requires 2 consecutive
    bad votes and a 1.5x leader margin).  The metric compresses the
    useful range: on this bucket a 26.9 Mbps socket scores 7.9M against
    a 167 Mbps leader at 6.62M -- a 1.19x ratio, below the 1.5x bar.
    The trigger never had a chance.

    - new tier fires on the first vote when last_rate_bps * 100 <
      reference * SLOW_VS_REF_PCT (10).  Same rate_ok and util_ok
      guards as other tiers.  Targets swap_tgt, which already picks
      second-best when the socket is already on the leader.
    - conn_state gains last_rate_bps (u64, bytes/sec), written on
      every vote, zeroed at every swap.  Not persisted; no
      STATE_VERSION bump.

  Fix: rate_app_limited is the app-limited bit; app_limited is a u32
  counter.  swapctx reads the bit now.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sat, 19 Sep 2026 17:00:00 +0000

bpftune (0.4.50) UNRELEASED; urgency=medium

  * swap trigger: skip swaps on sockets not using their window.
    Weekend data (192 swapctx samples, 2026-09-19): 60% of swaps
    fired at util<10%; 78% had ssthresh=INT_MAX (no loss ever);
    42% had packets_out<=1.  The trigger compared a raw metric that
    saturates on idle sockets against a util-filtered bucket leader
    -- the 0.4.40 util gate guarded the vote path only, not the
    fire condition.  Now util_ok gates both the desperate and
    moderate swap calls, same METRIC_MIN_UTIL_PCT=10 the vote path
    already uses.  Freeze is not gated (self-relative decision).
  * swapctx app_lim field: was reading tp->app_limited (u32 counter)
    instead of tp->rate_app_limited (u8:1 bitfield).  Log format
    unchanged; value becomes 0 or 1 as intended.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sat, 19 Sep 2026 14:30:00 +0000

bpftune (0.4.49) UNRELEASED; urgency=medium

  * swapctx gains app_lim and util; guard against downgrade swaps.
    - app_lim: kernel app_limited flag at swap time.  If set, the app
      isn't asking for more and a swap cannot help.
    - util: packets_out*100/cwnd, fraction of the window in use.
    - rate_ok: block a swap when the target alg's rate_ema on this
      bucket is below the source's.  Observed cookie 16102 swapped
      nv(rate_ema=13) -> reno(rate_ema=3) and died at 12% loss.

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sat, 19 Sep 2026 12:00:00 +0000

bpftune (0.4.48) UNRELEASED; urgency=medium

  * instrumentation: estab / swapctx / closport printks for weekend data
    collection.  Log-only, no behavior or state change.
    - estab: cookie, alg, forced(0/1), dest  -- the initial-pick denominator
    - swapctx: cwnd, ssthresh, packets_out, snd_wnd, on_leader, swap_count
      (companion to swap and freeze lines, joined by cookie)
    - closport: cookie, alg added -- joins socket lifetime across events

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Sat, 19 Sep 2026 00:00:00 +0000

bpftune (0.4.47) UNRELEASED; urgency=medium

  * swap/freeze printks: add dest=<IPv4> for per-bucket swap analysis
    directly from the log (log-only)

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Fri, 18 Sep 2026 20:55:28 +0200

bpftune (0.4.46) UNRELEASED; urgency=medium

  * midsamp: add alg= field so userspace can credit srate samples to
    the correct algorithm without joining through a met line

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Fri, 18 Sep 2026 16:14:30 +0200

bpftune (0.4.45) UNRELEASED; urgency=medium

  * swap target: use per-alg rate EMA instead of composite metric_value;
    metric still drives the trigger, rate drives the destination
  * swap log: add mt= (metric-would-have-picked) and rb= (rate-picked)
    for counterfactual comparison of outcomes
  * STATE_VERSION 14 -> 15

 -- cddeppe <91772433+cddeppe@users.noreply.github.com>  Fri, 18 Sep 2026 09:18:18 +0200

bpftune (0.4.43) unstable; urgency=medium

  [ Christophe Deppe ]
  * Fix the 0.4.41 origin-side gate: use data_segs_*, not segs_*.

    0.4.41 added a check to skip sockets where we are primarily
    receiving, gated on segs_out * 4 < segs_in.  segs_out includes
    pure ACKs; on a receiving socket ACKs dominate segs_out, so the
    ratio stayed above the threshold and the gate never fired.
    Measured on 09-17 post-deploy: 95.5% of swaps still origin-facing.

    Switch to data_segs_out / data_segs_in, which exclude pure ACKs
    and measure actual data direction.  Verified against a live ss
    dump: origin socket segs_out=2709/segs_in=9564 but
    data_segs_out=126/data_segs_in=9492 (fires); client socket the
    mirror (does not fire).

  * No other change.  Moving-socket suppression (also 0.4.41) is
    unaffected.

  [ bpftune ]
  * histogram-based delivered-rate reference (p99, population-decayed)

 -- bpftune <builder@example.com>  Thu, 17 Sep 2026 19:57:36 +0200

bpftune (0.4.41) UNRELEASED; urgency=medium

  * Skip origin-facing sockets in the vote path.

    A TCP proxy has two socket directions.  Client-facing (we send
    video to the user) is the one our CC controls.  Origin-facing (we
    receive video from a CDN) is not -- our CC on that socket governs
    HTTP requests and ACKs, not the download rate.  The tuner treated
    both identically; 0.4.39's diagnostic showed 57.7% of 09-16 swaps
    were origin-facing (88% on 09-17 morning).

    Vote path now skips sockets where segs_out * 4 < segs_in, i.e.
    where we are primarily receiving.  Client-facing sockets have
    segs_out >= segs_in and are unaffected.

  * Suppress moderate-tier swaps on a client-facing socket that is
    already in motion (falling or oscillating).

    Falling (metric dropping = delivered rate lifting) is the classic
    shape of an ABR probe: the player requested a higher rendition and
    the socket is climbing to meet it.  Swapping resets cwnd, shows a
    dip to the ABR measurement window, and the higher rendition is
    reverted.  Oscillating (metric bouncing) means a cwnd reset lands
    mid-swing and makes the next swing worse.

    Measured client-facing: falling n=6 win 0% loss 33%; oscillating
    n=17 win 12% loss 24%.  Both below the tier baseline (d=1 ~34%).

    After the origin gate above, every socket reaching the swap
    decision is sending-dominant, so no separate client-side check is
    required for this gate.

  * No struct change.  No state file change.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Thu, 17 Sep 2026 14:30:00 +0200

bpftune (0.4.40) UNRELEASED; urgency=medium

  * Utilization gate on the bucket EMA update.

    0.4.39's diagnostic showed half the votes on the heavy host
    were cast by sockets not using their cwnd-permitted window:
    packets_out/snd_cwnd median 0.08-0.10, ~50% under 0.10, only
    ~5% above 0.50.  On those votes the delivered rate is the
    rate the app supplied, not the rate the algorithm permitted,
    so the vote ranks algorithms on a quantity they did not set.
    That is consistent with the 34-65% spread between best and
    worst algorithm on busy buckets, versus the ~200% a genuine
    difference would show.

    The EMA update in the vote path now skips sockets whose
    packets_out/snd_cwnd is below 10%.  metric_count still
    increments so coverage / MIN_LEADER_TRUST semantics are
    unchanged; only the metric_value EMA is not updated.

    BBR is exempt.  BBR deliberately keeps snd_cwnd large and
    paces separately, so packets_out/snd_cwnd is low on a busy
    BBR socket.  snd_cwnd == 0 (no cwnd info on the hook) is
    also treated as allowed.

    Sentinel metrics (0 unset, ~0 poisoned) can still win the
    ESTABLISHED min loop, same as before; both consumers of
    minindex already check metric_count > 0 or the value
    sentinel before use, so no filter is needed in the loop.
    An earlier draft added a skip filter and blew the verifier
    to the 1M instruction limit (16-way unrolled loop with
    unprovable value ranges); it was reverted.

    No struct change.  No state file change.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Wed, 16 Sep 2026 20:00:00 +0200

bpftune (0.4.39) UNRELEASED; urgency=medium

  * Diagnostic: emit snd_cwnd and packets_out on each vote.

    The live trace on the heavy host showed the kernel's app_limited
    flag set on ~96% of sockets.  If that holds, most votes measure
    the app's supplied rate rather than the path's capacity, and the
    per-algorithm bucket EMAs would converge on the app's rate
    instead of the algorithm's capability.  The observed 34-65%
    spread between best and worst algorithm on busy buckets
    (versus the 200%+ a genuine difference would show) is
    consistent with this dilution, but is not by itself enough to
    prove it.

    Fix can't be designed until the size of the problem is known,
    and the app_limited flag is not exposed to BPF.  packets_out vs
    snd_cwnd is the proxy: when packets_out << snd_cwnd the app has
    not filled the window the algorithm permits.  Both fields are
    on bpf_tcp_sock and already in scope at the vote site.

    A second printk line joins by cookie to the existing met line.
    Split because the met line is already at the 12-argument printk
    limit.  Doubles the met log volume for the duration of the
    diagnostic.

    No metric logic change.  No struct change.  No state file change.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Wed, 16 Sep 2026 17:00:00 +0200

bpftune (0.4.38) UNRELEASED; urgency=medium

  * Fix the 0.4.37 flat-gate: relax the required sample count
    from 3 to 2.

    0.4.37 introduced a flat-socket gate on the moderate swap tier
    that required all three of {last_metric, hist_1, hist_2} to be
    populated before it could classify a socket.  Root cause of the
    failure: SWAP_BAD_FIRST=2 fires the first moderate swap after
    two consecutive bad votes.  On a fresh socket -- or one whose
    history was zeroed by a prior swap -- hist_2 is still 0 at the
    moment the swap fires.  The gate therefore never fired on the
    exact population it was designed to suppress: 0.4.37 moderate
    swap rate was unchanged from 0.4.36 (7 fires in 3h vs pre-gate
    baseline of ~47/day).

    Validation from the 09-14 + 09-15 pre-gate logs, binning swaps
    by number of samples on the current algorithm:

        2sample-flat      n=85    win ~10%   <- gate now catches
        2sample-nonflat   n=17    win ~48%   <- max/min check excludes
        1sample           n=218   win ~38%   <- insufficient, falls
                                              through (unchanged)
        3sample           n=57    win mixed  <- unchanged

    Data confirms 2sample-flat is the same population as 3sample-
    flat by win rate (~10% vs ~8%), well below the tier target.
    2sample-nonflat stays at 45-50% and is not gated.

  * hist_1/hist_2 remain in struct conn_state; swap-path resets
    unchanged.  No state-format change: STATE_VERSION stays at 12,
    no state file delete on deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 22:00:00 +0200

bpftune (0.4.37) UNRELEASED; urgency=medium

  * Flat-socket gate on the moderate swap tier.
    Over 340 swaps in the 09-14 + 09-15 met logs, binning by the
    shape of the socket's own last three metric samples shows flat
    sockets win only 7.9% (90.6% null, 1.6% loss) -- inside the
    metric's own noise band.  Rising runs 57.9% win, oscillating
    16.7%, falling 27.3%.  The moderate tier selects for flat:
    78.5% of its fires are flat sockets vs 21.9% in desperate.  A
    socket that is deteriorating climbs through 1.5x-2.0x fast and
    trips the desperate bar at 2.0x; a flat socket sits at a fixed
    ratio and eventually trips moderate on the 2-bad-check rule.
  * Add hist_1/hist_2 to struct conn_state; the vote path shifts
    the previous two samples and the swap path resets them.  Gate
    the moderate-tier swap on max/min of the three >= 1.10.
  * Desperate and insufficient-history sockets are untouched:
    desperate fires on the 2x rule regardless of shape, and a
    socket with fewer than three samples has not been judged yet
    (42.4% win rate, the largest source of real wins besides
    rising).
  * Sk_storage only (conn_state); the persisted state file layout
    is unchanged, STATE_VERSION stays at 12, no state file delete
    needed on deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 20:00:00 +0200

bpftune (0.4.36) UNRELEASED; urgency=medium

  * Userspace re-anchor for the tcp_conn_tuner tracker.
    The BPF vote path maintains best_i/best_v incrementally and the
    ESTABLISHED-time rescan refreshes it on new connections, but
    between those events a non-leader metric can drift to a lower
    value without being promoted.  Observed on the home bucket:
    best_i pointed at htcp at 1.79M while dctcp at 608K had n=5 and
    was never promoted, so swaps targeted a non-leader.
  * Add a daemon worker thread that every 30s walks remote_host_map,
    finds the trusted minimum (metric_count >= MIN_LEADER_TRUST) and
    second-best, and writes best_i/best_v/second_i/second_v back
    via bpf_map_update_elem.  Read-modify-write preserves the
    reference-fix fields (rate_high_streak etc.) and every other
    field in the struct.
  * No BPF change, no state version change.  The swap path keeps
    reading the same tracker fields it reads today.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 18:00:00 +0200

bpftune (0.4.35) UNRELEASED; urgency=medium

  * Collapse the split settle window into a single constant.
    0.4.33 introduced T_SETTLE_NORMAL_NS (60s) / T_SETTLE_DESPERATE_NS
    (10s), chosen by whether the socket was desperate or had hit
    persist_bad >= 2.  In practice the 60s branch was unreachable:
    any moderate swap requires SWAP_BAD_FIRST/LATER = 2 consecutive
    above-margin checks, which is exactly the condition that sets
    persist_bad >= 2.  So every swap took the 10s branch, and the
    60s branch was dead code.
  * Remove the persist_bad counter, PERSIST_BAD_THRESHOLD and both
    split constants.  The socket-level truth is: after 2 consecutive
    bad checks we already know the socket is stuck; a full-minute
    wait between retries was never useful.
  * conn_state no longer carries persist_bad.  STATE_VERSION
    11 -> 12.  State file MUST be deleted on deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 17:00:00 +0200

bpftune (0.4.34) UNRELEASED; urgency=medium

  * Moderate-tier margin raised from 1.25x to 1.50x.
    Observed: on buckets where top algorithms are within ~10%
    of each other (TIGHT x1.0/x1.1), the 1.25x-1.50x band is
    inside the metric's noise.  Swaps fired in that band landed
    below a coin flip (12 IMP / 15 WRS over one day).  Raising
    the floor to 1.50x keeps the tier for meaningful moderate
    cases (1.50-2.00x) and drops the noise band.
  * Post-freeze desperate tier now judges against the socket's
    own best_seen_metric, not the bucket leader.  Observed: a
    frozen socket (cookie 2927) cycled eight more times through
    bic/veno/illinois/highspeed/htcp while every algorithm
    produced 12-14M -- the socket was at its path ceiling, not
    algorithm-limited.  The bucket-relative 2x test kept firing
    because the bucket best_v was 5.6M (other sockets ran
    faster on the same path).  Now: a frozen socket only gets a
    post-freeze desperate swap if last_metric >= best_seen * 2.
    Pre-freeze behavior unchanged.
  * No struct changes.  STATE_VERSION unchanged.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 16:00:00 +0200

bpftune (0.4.33) UNRELEASED; urgency=medium

  * Shorter settle window for desperate and persistent-bad sockets.
    Desperate tier (>= 2x leader) and sockets above margin on
    PERSIST_BAD_THRESHOLD (2) consecutive evaluations use
    T_SETTLE_DESPERATE_NS (10s) instead of T_SETTLE_NORMAL_NS
    (60s).  A long-lived stuck socket no longer waits a full
    minute between swap attempts.
  * conn_state: settle_until -> last_swap_at (raw timestamp;
    window computed per tick), plus persist_bad counter.
  * STATE_VERSION 10 -> 11.  State file MUST be deleted on
    deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 15:00:00 +0200

bpftune (0.4.32) UNRELEASED; urgency=medium

  * Reference-drift fix: gate reference updates on sustained-transfer
    evidence, and allow the reference to recover upward via a streak
    of 5 consecutive far-better readings.
    - Down-drift observed: DNS, ctrl-API, SSH keepalives deliver
      tiny rates (88K-350K).  Their heal-down rule (rate < ref/3)
      pulled max_rate_delivered toward control-plane rates.
      Overnight on the home bucket this collapsed the ref from
      ~20M to 1.68M.
    - Up-lock observed: once collapsed, real traffic at 5-10M sits
      above ref*2 = 3.36M, so the outlier-rejection rule refused to
      accept it.  Nothing could move the ref up.
    - Fix part 1: new REF_TIME_CHECK_MIN_SEGS (2000) gates
      time-check reference updates.  Segment-rung votes (>= 10K
      segments) pass naturally.  Control-plane sockets cannot
      reach 2000.
    - Fix part 2: streak promotion.  Consecutive readings far past
      the ref (rate > ref*2, or rtt < ref/2) accumulate; at
      REF_HIGH_STREAK_N (5) in a row, promote the reference to the
      max (rate) or min (rtt) of the streak.  A single anomalous
      reading is still rejected.
    - Fix part 3: the same gate applies to the heal-down (rate)
      and heal-up (rtt) paths.  Control sockets cannot drag the
      reference in either direction.
  * struct remote_host gains rate_high_streak, rate_high_max,
    rtt_low_streak, rtt_low_min.
  * STATE_VERSION 9 -> 10.  State file MUST be deleted on
    deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Tue, 15 Sep 2026 12:00:00 +0200

bpftune (0.4.31) UNRELEASED; urgency=medium

  * Per-socket freeze after FREEZE_AFTER_SWAPS (4) swaps.
    A socket that has cycled through four different algorithms
    without stabilising has a path or application problem that
    no algorithm choice can fix.  Once swap_count reaches the
    threshold, swap to whichever algorithm produced the lowest
    metric on THIS socket, set frozen=1, and skip moderate swaps
    for the rest of the connection.  Desperate readings (>= 2x the
    leader) still fire: the freeze insulates only against moderate
    churn, not against genuine path degradation.
  * conn_state gains best_seen_metric, best_seen_alg, frozen.
    best_seen_metric/alg update on every vote; the freeze path
    reads them.  Skips the ~0 poisoned-metric and 0 unset
    sentinels.
  * freeze log line emitted (tag=freeze) so the pattern is
    visible in the met log alongside swap lines.
  * STATE_VERSION 8 -> 9.  State file MUST be deleted on
    deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Mon, 14 Sep 2026 22:00:00 +0200

bpftune (0.4.30) UNRELEASED; urgency=medium

  * Two-tier swap trigger.
    - Desperate tier: socket_metric >= best_v * 2.00 fires
      immediately, regardless of bad_checkpoints.  Very bad
      sockets do not wait.
    - Normal tier: socket_metric >= best_v * 1.25 requires
      SWAP_BAD_FIRST (2, changed from 1) consecutive bad
      checkpoints on the first swap; SWAP_BAD_LATER (2) on
      subsequent swaps.
    Observed churn: on a bucket where the top-8 algorithms were
    within 20% of each other, sockets on 1.25-1.5x readings
    swapped back and forth as the leader moved -- 137 swaps/day
    on the heavy host, with a dip in user experience on YouTube
    Shorts.  The two-tier split targets that: sockets in the
    churn band wait for confirmation, sockets genuinely stuck
    fire immediately.
  * Swap printk extended with bc= (bad_checkpoints at fire),
    ac= (source algorithm bucket vote count), and d= (1 = fired
    via desperate tier, 0 = normal tier).  So we can measure
    which tier produced which outcome over the next day.
  * No STATE_VERSION bump (no struct field changes).

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Mon, 14 Sep 2026 20:00:00 +0200

bpftune (0.4.29) UNRELEASED; urgency=medium

  * Add a time-based supplementary vote: every 60 seconds, if the
    socket has advanced by >= 1000 segments since the last time
    check, the vote + swap decision runs even if no segment
    checkpoint was reached.  Fixes the case where a long-lived
    socket (past the 1M rung, where nextp is ~0) can go many
    minutes without re-evaluation while the path degrades.
  * Raise the post-swap settle window from 5s to 60s, matching
    the time-check cadence.  This is the rate limiter now.
  * Remove SWAP_MAX.  No total cap per socket; a long socket can
    swap as often as needed, at most once per settle window.
  * Swap decision: when the socket is on best_i and best_i's
    metric is worse than second_i's by 1.25x for this socket,
    swap to second_i.  Handles the case where the leader is
    performing badly for a particular socket but the bucket's
    EMA hasn't caught up yet.  Because second_v >= best_v, the
    1.25x bar is naturally higher than the normal case, so this
    fires rarely.
  * STATE_VERSION 7 -> 8 (struct conn_state gained two fields).
    State file MUST be deleted on deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Mon, 14 Sep 2026 16:00:00 +0200

bpftune (0.4.28) UNRELEASED; urgency=medium

  * Add loss rate as a metric axis.  metric is now
    rtt_term + rate_term + loss_term, where loss_term =
    min(retrans/segs_out, LOSS_CAP_BP) / LOSS_CAP_BP * LOSS_SCALE
    with LOSS_CAP_BP=200 (2.00%).  The met log line gains a loss=
    field so the distribution can be observed live.
  * Remove the hardcoded forced-BBR rescue in RETRANS_CB.  It
    predated the per-destination metric and bypassed it: any
    socket above ~1.56% loss was switched to BBR and its RTT_CB
    flag cleared, so lossy sockets could never vote for any other
    algorithm.  With loss now in the metric, the tuner can learn
    per-path which algorithm handles loss best; the forced BBR
    shortcut is no longer needed.  The TCP_THIN_LINEAR_TIMEOUTS
    opt-in for lossy sockets is kept.
  * STATE_VERSION 6 -> 7 (metric formula changed; old bucket
    values are not comparable).  State file MUST be deleted on
    deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Mon, 14 Sep 2026 14:00:00 +0200

bpftune (0.4.27) UNRELEASED; urgency=medium

  * Re-anchor best_i/best_v on every ESTABLISHED.  The incremental
    tracker in the vote path only compares best against second, so a
    third algorithm can drift into a lower position without ever being
    promoted through the pair.  Observed on the fleet as best_i
    disagreeing with the top-3 output.  Fix persists minindex and
    metric_min (already computed by the existing selection loop) into
    remote_host->best_i/best_v.  No new variables — an earlier attempt
    with an explicit min2index blew the 1M insn verifier budget.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Mon, 14 Sep 2026 15:00:00 +0200

bpftune (0.4.26) UNRELEASED; urgency=medium

  * Merge vote+swap back to a single RTT_CB program.  set_cong is now
    called inline from the vote path, removing the pending_swap field
    and the cross-program queue.  Swaps fire in the same RTT_CB event
    as the decision instead of on a later event.  Program count: 3 -> 2.
  * METRIC_TRIGGER_SEGS 5000 -> 10000.  The 5K experiment biased the
    metric: every socket 5K sample is roughly 10x lower than its 25K
    sample, so 5K samples dominated the leader ranking and cubic
    took over on n=1.
  * MIN_LEADER_TRUST=3.  A leader needs at least 3 votes before it
    can be a swap target.
  * No downgrade branch.  A socket already on best_i has no better
    target and is skipped; previously the code targeted second_i, a
    strict downgrade that could thrash between best and second-best.
  * SWAP_BAD_FIRST=1, SWAP_BAD_LATER=2.  First rescue fires on a
    single bad checkpoint (faster rescue for 30s+ videos); subsequent
    swaps still require two, to prevent thrash.
  * STATE_VERSION 5 -> 6: pending_swap removed.  State file must be
    deleted on deploy.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Mon, 14 Sep 2026 05:00:00 +0200

bpftune (0.4.25) UNRELEASED; urgency=medium

  * Mid-socket algorithm swap: when a socket is assigned a bottom-half
    algorithm at ESTABLISHED (rank_at_assign >= 8), and its metric stays
    >= 1.25x the best alternative for 2 consecutive voting checkpoints,
    swap it to that alternative via bpf_setsockopt(TCP_CONGESTION) from
    a dedicated RTT_CB program.  Protects against exploration regret:
    the ~5% of sockets greedy assigns to a random algorithm no longer
    stay on a bad pick for their entire life.  Bounded by MAX_SWAPS=2
    and a 5s settle window after each swap.
  * Split the single sockops program into three (conn_tuner /
    conn_tuner_vote / conn_tuner_swap).  The merged program with the
    swap decision in RTT_CB exceeded the 1M instruction verifier
    budget via state explosion: a 16-iter loop adjacent to two live
    map_value pointers plus helper calls.  Snapshotting best_alt_i at
    ESTABLISHED removes all loops from the vote path; the swap exec
    path is straight-line.
  * sk_storage_map value type: __u64 -> struct conn_state { state,
    swaps, bad_count, settle_until, pending_swap, best_alt_i,
    rank_at_assign }.

  Userspace attaches and detaches all three programs at the same
  cgroup sockops hook.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 23:00:00 +0200

bpftune (0.4.25) UNRELEASED; urgency=medium

  * Mid-socket algorithm swap.  When a socket is assigned a bottom-half
    algorithm at ESTABLISHED (rank_at_assign >= 8 by current bucket
    metric), and its metric stays >= 1.25x the best alternative for 2
    consecutive voting checkpoints, swap it to that alternative via
    bpf_setsockopt(TCP_CONGESTION) from a dedicated RTT_CB program.
    Protects against exploration regret: the ~5% of sockets the greedy
    path assigns to a random algorithm no longer stay on a bad pick
    for their entire life.  Bounded by MAX_SWAPS=2 and a 5s settle
    window after each swap.
  * Split the single sockops program into three (conn_tuner /
    conn_tuner_vote / conn_tuner_swap).  The merged program with the
    swap decision in RTT_CB exceeded the 1M instruction verifier
    budget via state explosion: a 16-iter loop adjacent to two live
    map_value pointers plus helper calls.  Snapshotting best_alt_i at
    ESTABLISHED removes all loops from the vote path; the swap exec
    path is straight-line.  Total states across the three programs:
    1694, below the single-program baseline of 1217+ additions.
  * sk_storage_map value type: __u64 -> struct conn_state { state,
    swaps, bad_count, settle_until, pending_swap, best_alt_i,
    rank_at_assign }.

  Userspace attaches and detaches all three programs at the same
  cgroup sockops hook.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 23:00:00 +0200

bpftune (0.4.25) UNRELEASED; urgency=medium

  * Mid-socket algorithm swap.  When a socket is assigned a bottom-half
    algorithm at ESTABLISHED (rank_at_assign >= 8 by current bucket
    metric), and its metric stays >= 1.25x the best alternative for 2
    consecutive voting checkpoints, swap it to that alternative via
    bpf_setsockopt(TCP_CONGESTION) from a dedicated RTT_CB program.
    Protects against exploration regret: the ~5% of sockets the greedy
    path assigns to a random algorithm no longer stay on a bad pick
    for their entire life.  Bounded by MAX_SWAPS=2 and a 5s settle
    window after each swap.
  * Split the single sockops program into three (conn_tuner /
    conn_tuner_vote / conn_tuner_swap).  The merged program with the
    swap decision in RTT_CB exceeded the 1M instruction verifier
    budget via state explosion: a 16-iter loop adjacent to two live
    map_value pointers plus helper calls.  Snapshotting best_alt_i at
    ESTABLISHED removes all loops from the vote path; the swap exec
    path is straight-line.  Total states across the three programs:
    1694, below the single-program baseline of 1217+ additions.
  * sk_storage_map value type: __u64 -> struct conn_state { state,
    swaps, bad_count, settle_until, pending_swap, best_alt_i,
    rank_at_assign }.

  Userspace attaches and detaches all three programs at the same
  cgroup sockops hook.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 23:00:00 +0200

bpftune (0.4.24) UNRELEASED; urgency=medium

  * Vote at every checkpoint at or above METRIC_TRIGGER_SEGS, not just
    10K.  A socket that survives to 1M segments now casts five votes
    (10K/25K/100K/500K/1M) instead of one.  Long-lived sockets weigh
    more than short ones in proportion to their evidential value — the
    30-minute video and the 200 ms TLS handshake no longer contribute
    equally.  METRIC_AVG_CAP=32 bounds any single socket to ~15%% of an
    algorithm mean, so proportional weighting cannot drown the pack.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 19:00:00 +0200

bpftune (0.4.23) UNRELEASED; urgency=medium

  * Fix cold-start coverage to count selections, not votes.  The 0.4.22
    coverage loop gated on per-algorithm metric_count, which only
    increments when a socket actually votes (at 10K segments, or at
    close above 100 segments).  Sockets that never vote did not advance
    the coverage pointer, so the same algorithm was assigned to every
    new connection until one of its sockets happened to vote.  Observed
    on the heavy host minutes after 0.4.22 deploy: cubic 149, htcp 99,
    bbr 84, and indices 6-15 at zero.
  * Add __u64 selection_count to struct remote_host.  Increment on
    every ESTABLISHED; force-pick algorithm (selection_count & 15)
    while selection_count < 32.  Now the first 32 connections to a
    bucket get exactly two of each algorithm, regardless of whether
    any socket votes.  STATE_VERSION 3 -> 4.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 18:30:00 +0200

bpftune (0.4.22) UNRELEASED; urgency=medium

  * Margin-gated exploitation.  Track leader and second-place metric in
    one pass (no extra iterations); if the leader is not >=25% ahead of
    second, boost exploration from 1/20 to 1/4 until a clear winner
    emerges.  Prevents the tuner from committing to a leader whose
    early samples look better than they are.
  * Coverage gate lowered from 5 back to 2.  Coverage=5 required 80
    sockets per bucket before exploit could start, which most
    destinations never reach.  Coverage=2 is enough to establish a
    first-pass distribution; the margin gate then decides whether the
    evidence supports committing.
  * Remove tie-break machinery from the selection path.  With
    cold-start coverage guaranteeing every algorithm has real samples,
    the multi-candidate random selection and its 16-case switch are
    dead weight; on a program already at the 1,000,000-instruction
    verifier limit, dropping them is what makes room for the margin
    gate.  Ties now resolve by lowest index.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 17:30:00 +0200

bpftune (0.4.21) UNRELEASED; urgency=medium

  * metric_value: replace slow-learning rolling update with count-based
    incremental mean (divisor min(count+1, METRIC_AVG_CAP=32)). Previous
    update applied a fixed 25% step per observation, giving the metric an
    effective memory of ~4 samples regardless of sample count. Home-bucket
    leader churn observed 2026-09-13 (bbr 5.8M -> 7.59M over 5 votes) was
    this step size, not path drift.
  * Skip addresses that do not benefit from CC tuning: 127.0.0.0/8,
    169.254.0.0/16, ::1, fe80::/10.
  * Add remote_port to midsamp and closport printks, so incoming 443
    connections can be paired reliably.
  * Cold-start coverage: on a fresh bucket, force-sample the
    least-sampled algorithm until every algorithm has >= 2 votes,
    then fall back to normal epsilon-greedy.  Without this, a lucky
    first sample locks the leader in and the exploration rate (5%/16
    per non-leader) is too thin to recover.  Bounded cost: at most 32
    sockets, then zero overhead.
  * Raise cold-start coverage from 2 to 5 votes per algorithm before
    exiting the round-robin phase, so the tuner does not begin
    exploiting a leader whose early samples may have been lucky.
    Bounded cost: 80 sockets per fresh bucket, then zero overhead.
  * Remove dead per-algorithm min_rtt / max_rate_delivered fields from
    struct tcp_conn_metric. Displayed but never read by the metric.
    STATE_VERSION 2 -> 3.

  DEPLOY: the state file MUST be deleted between stop and start.
  Metric semantics changed (new averaging) and the struct changed
  (STATE_VERSION 3). Preserving state would poison the new average.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 23:00:00 +0200

bpftune (0.4.20) UNRELEASED; urgency=medium

  * Fixed-size metric sampling: a socket casts its metric vote once
    at METRIC_TRIGGER_SEGS (10000) segments, so all voters are
    compared at a comparable lifecycle stage.  Previously the vote
    only happened at close, so a 127-segment health check and a
    500,000-segment video transfer contributed equally.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 20:00:00 +0200

bpftune (0.4.19) UNRELEASED; urgency=medium

  * Diagnostic: log one mid-flight sample per socket at each of
    1K/5K/25K/100K/500K/1M segments via BPF_SOCK_OPS_RTT_CB, plus a
    port-tagged close sample.  Lets us compare mid-flight metric
    evolution against the close-time value for the same socket.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sun, 13 Sep 2026 09:13:38 +0200

bpftune (0.4.18) UNRELEASED; urgency=medium

  * Switch bucket key from next-hop gateway to destination IP, matching
    the metric's stated intent (per remote host).  Previously all IPv4
    traffic through the default route shared one bucket, mixing fast
    local CDN paths with slow tunnel paths.
  * Switch remote_host_map to LRU hash, 4096 entries.  Self-manages
    eviction on a destination-keyed workload.
  * Persist only buckets with >= PERSIST_MIN_INSTANCES (8) connections.
    Recurring paths survive restarts; one-off destinations do not.
  * Bump STATE_VERSION to 2: prior state files held gateway-addressed
    keys, which are meaningless under destination keying.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 22:57:56 +0200

bpftune (0.4.17) UNRELEASED; urgency=medium

  * Diagnostic: log heal events via bpf_printk in the BPF caller.
    tcp_metric_calc returns healed reference values through output
    parameters; the BPF caller prints them when non-zero.  Keeps
    bpf_printk out of the shared header.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 20:22:02 +0200

bpftune (0.4.16) UNRELEASED; urgency=medium

  * Fix: heal poisoned bucket references.  When a socket's min_rtt
    is more than REF_HEAL_FACTOR times the current reference (or its
    rate is more than REF_HEAL_FACTOR below the ceiling), move the
    reference 1/REF_HEAL_DIV of the gap toward the observed value.
    Prevents a single fast socket that hit an empty bucket first from
    permanently capping the RTT term for all subsequent traffic.
    Healthy traffic never triggers this.  Parameters: factor 3, div 16.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 20:05:28 +0200

bpftune (0.4.15) UNRELEASED; urgency=medium

  * Fix: reject bucket-reference updates that are more than 2x better
    than the current reference.  A single anomalous socket (e.g. a
    fast CDN hitting the same gateway as a 30ms tunnel) had been
    able to permanently anchor min_rtt far below real traffic and
    saturate rtt_term at its cap.  Also guard rate_term against
    negative when a socket exceeds the ceiling without updating it.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 19:25:56 +0200

bpftune (0.4.14) UNRELEASED; urgency=medium

  * Fix: change RTT term to measure queueing delay (avg_rtt - bucket
    min_rtt) instead of min-against-min, which was path-constant and
    carried no algorithm signal on stable paths. Adds avg_rtt parameter
    to tcp_metric_calc, sourced from tp->srtt_us >> 3.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 17:42:03 +0200

bpftune (0.4.13) UNRELEASED; urgency=medium

  * Fix: pass current socket's rate_delivered to tcp_metric_calc
    instead of the algorithm's all-time-best rate. Previously the
    rate term was frozen per algorithm (only changed when the alg
    set a new personal best), making half the metric historic while
    the RTT half was live per-socket.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 17:09:38 +0200

bpftune (0.4.12) UNRELEASED; urgency=medium

  * Fix: only sockets with >= METRIC_MIN_SEGS total segments update
    the per-algorithm metric and bucket references. Prevents small
    control sockets from pinning the bucket min_rtt to an unachievable
    floor, which had caused every real connection to saturate the RTT
    term and lock the tuner onto whichever algorithm got the poisoned
    bucket first.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 16:54:55 +0200

bpftune (0.4.11) UNRELEASED; urgency=medium

  * Diagnostic: log raw socket min_rtt and bucket min_rtt alongside the
    metric terms.  Remove duplicate old-format printk.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 16:42:33 +0200

bpftune (0.4.10) UNRELEASED; urgency=medium

  * Diagnostic: log rtt_term and rate_term separately in metric printk.
    Branch only (diag/metric-terms); do not merge to main.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 16:30:00 +0200

bpftune (0.4.9) UNRELEASED; urgency=medium

  * Custom fork: expanded TCP congestion control selection, persistent
    learned state, and instrumentation for metric weight analysis.

 -- Christophe Deppe <cddeppe@users.noreply.github.com>  Sat, 12 Sep 2026 15:54:12 +0200

bpftune (0-1) UNRELEASED; urgency=medium

  * Initial release. (Closes: #nnnn)  <nnnn is the bug number of your ITP>

 -- Bernd Zeimetz <bzed@debian.org>  Thu, 28 Nov 2024 21:39:29 +0100
